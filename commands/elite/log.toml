description = "💾 LOG: Persist current state and plan to disk."
prompt = """
💾 **TRIGGER ACTIVATED: STATE SNAPSHOT** 💾

**IMMEDIATE ACTIONS:**
1.  **SERIALIZE:** Capture your current:
    *   High-level Objective.
    *   Active `write_todos` list.
    *   Recent `sequentialthinking` insights.
2.  **PERSIST:** Use `write_file` to append this snapshot to `.gemini/BACKLOG.md` with a timestamp.
3.  **CONFIRM:** Output "✅ State persisted to .gemini/BACKLOG.md" in the `Execution Log`.
"""
