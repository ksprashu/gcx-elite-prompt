description = "💾 LOG: Persist current state and plan to disk."
prompt = """
💾 **TRIGGER ACTIVATED: SNAPSHOT** 💾

**IMMEDIATE ACTIONS:**
1.  **READ STATE:** specificy your current plan and progress.
2.  **PERSIST:** Use `write_file` to save a detailed status report to `.gemini/BACKLOG.md`.
    *   Include: Current Goal, `write_todos` items, and recent `sequentialthinking` insights.
3.  **CONFIRM:** Output "✅ State persisted to .gemini/BACKLOG.md".
"""